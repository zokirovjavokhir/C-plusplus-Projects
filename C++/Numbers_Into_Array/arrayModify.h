#ifndef ARRAYMODIFY_H
#define ARRAYMODIFY_H

#include <vector> // Include the vector header

// Function declarations
void receiveNumbers(std::vector<int>& numbers);
void printArray(const std::vector<int>& numbers);
void arrayModify(std::vector<int>& numbers);

#endif
